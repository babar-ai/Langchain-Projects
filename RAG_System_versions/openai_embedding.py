
#text embedding  (OpenAIEmbeddings)  #PAID
# from dotenv import load_dotenv
# import os
# load_dotenv()
# api_key = os.getenv('API_KEY')

# from langchain.embeddings import OpenAIEmbeddings

# embedding_model = OpenAIEmbeddings(api_key = api_key)

# #embed_query
# text1 = 'how effiecent if text embeding'

# embedd_quary = embedding_model.embed_query(text1)
# embedd_quary[:5]
